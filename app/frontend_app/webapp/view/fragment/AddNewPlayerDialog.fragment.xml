<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog id="addNewPlayerDialog" title="{i18n>addNewPlayer}" class="sapUiTinyMargin">
        <Input required="true" id="playerName" placeholder="playerName" showSuggestion="true"/>
        <Input required="true" id="playerLastName" placeholder="playerLastName" showSuggestion="true" />

        <Slider id="playerAge" value="16" width="100%" min="16" max="50" showAdvancedTooltip="true" showHandleTooltip="false" />

        <Select id="playerPosition" width="100%" required="true" selectedKey="{/SelectedKey}" forceSelection="false" items="{
						path: 'SelectModel>/'
					}">
            <core:ListItem text="{SelectModel>Name}" icon="{SelectModel>Icon}"/>
        </Select>
        <Bar>
            <contentRight>
                <Button id="confirmBtn" icon="sap-icon://accept" text="{i18n>submit}" press="addPlayer" />
            </contentRight>
            <contentLeft>
                <Button id="cancelBtn" icon="sap-icon://decline" text="{i18n>cancel}" press="onPressCancel" />
            </contentLeft>
        </Bar>
    </Dialog>
</core:FragmentDefinition>